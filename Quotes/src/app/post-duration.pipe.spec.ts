import { PostDurationPipe } from './post-duration.pipe';

describe('PostDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new PostDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
